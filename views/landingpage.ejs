<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Shramsetu</title>
  <style>
    /* Reset and base styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: ui-sans-serif, system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
      font-size: 16px;
      font-weight: 400;
      line-height: 1.5;
      color: rgb(2, 8, 23);
    }
    /* Layout */
    .container {
      width: 100%;
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 16px;
    }

    /* Header styles */
    header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 10px;
      background-color: #1E2D3D;
    }
    .logo {
      display: flex;
      align-items: center;
      gap: 10px;
     background-color: transparent; /* Remove gray background */
    }
  .logo img {
     width: 100px; /* Or any size you prefer */
    height: auto;
    }
    .logo-accent {
      color: rgb(37, 99, 235);
    }

    nav {
      display: flex;
    }

    .nav-buttons, .action-buttons {
      display: flex;
    }

    /* Button styles */
    .button {
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: #ffce9a;
      border-radius: 9999px;
      border: none;
      font-size: 14px;
      font-weight: 500;
      line-height: 20px;
      padding: 8px 24px;
      height: 40px;
      white-space: nowrap;
      transition: all 0.15s cubic-bezier(0.4, 0, 0.2, 1);
      cursor: pointer;
      margin-left: 16px;
    }

    .button:first-child {
      margin-left: 0;
    }

    .button-outline {
      border: 0.666667px solid rgb(226, 232, 240);
      padding: 24px 32px;
    }

    /* Main content */
    main {
      padding: 48px 16px;
    }

    .hero-section {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 48px;
      align-items: center;
    }

    .hero-content h1 {
      font-size: 96px;
      font-weight: 900;
      letter-spacing: -4.8px;
      line-height: 96px;
      color:#1E2D3D;
    }

    .hero-buttons {
      display: flex;
      flex-wrap: wrap;
      gap: 16px;
      margin-top: 24px;
    }

    /* Card styles */
.card-container {
  display: flex;
  align-items: center;
  position: relative;
  margin-left: 70px;
}

.image-slider {
  display: flex;
  overflow: hidden;
  width: 500px;
}

.image-wrapper {
  flex: 0 0 100%;
  display: none; /* Hide all images initially */
}

.image-wrapper img {
  width: 500px;
  height: 400px;
  object-fit: cover;
  border-radius: 8%;
}

.image-wrapper.active {
  display: block; /* Show only the active image */
}

.arrow {
  border: 1px solid black;
  border-radius: 50%;
  font-size: 12px;
  cursor: pointer;
  padding: 5px;
}
    .service-card {
      background-color: #ffce9a;
      padding: 16px;
      border-radius: 8px;
      box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
    }

    .service-title {
      font-weight: 500;
    }

    .service-description {
      font-size: 14px;
      color: rgb(107, 114, 128);
    }

    /* Responsive styles */
    @media (max-width: 1024px) {
      .hero-section {
        grid-template-columns: 1fr;
      }

      .hero-content h1 {
        font-size: 64px;
        letter-spacing: -3px;
        line-height: 70px;
      }
    }

    @media (max-width: 768px) {
      header {
        flex-wrap: wrap;
      }

      .nav-buttons {
        display: none;
      }

      .action-buttons {
        margin-top: 16px;
        width: 100%;
        justify-content: center;
      }

      .hero-content h1 {
        font-size: 48px;
        letter-spacing: -2px;
        line-height: 54px;
      }
    }
      .container1 {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
  
      padding: 16px;
      background-color:#1E2D3D;
    }
    
    /* Image sections container */
    .image-sections {
      display: flex;
      flex-direction: column;
      width: 100%;
      max-width: 896px;
      gap: 32px;
    }
    
    /* Common section styles */
    .section {
      display: flex;
      width: 100%;
      border-radius: 24px;
      overflow: hidden;
    }
    
    /* Gray box styles */
    .gray-box {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 66.6667%;
      height: 256px;
      background-color: rgb(209, 213, 219);
      padding: 24px;
      color: rgb(75, 85, 99);
      font-size: 18px;
      font-weight: 400;
      line-height: 28px;
    }
    
    /* Red box styles */
    .red-box {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 33.3333%;
      height: 256px;
      background-color: rgb(248, 113, 113);
      object-fit: cover;
      padding: 24px;
      font-size: 18px;
      font-weight: 400;
      line-height: 28px;
      box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
    }

    /* Top section red box has white text */
    .top .red-box {
      color: rgb(255, 255, 255);
    }
    
    /* Bottom section red box has black text */
    .bottom .red-box {
      color: rgb(0, 0, 0);
    }
    .ending{
      height:120px;
      width:max-content;
      background-color: #1E2D3D;
    }
    
    /* Responsive styles for small screens */
    @media (max-width: 640px) {
      .section {
        flex-direction: column;
      }
      
      .gray-box,
      .red-box {
        width: 100%;
        height: 192px;
      }
    }
  </style>
</head>
<body>
  <div class="page-wrapper">
    <header>
      <div class="logo"><img src="/images/logo.png">
      </div>
      
      <nav class="nav-buttons">
        <button class="button service">Services</button>
        <button class="button about">About</button>
      </nav>
      
      <div class="action-buttons">
        <button class="button login_user">Login</button>
        <button class="button signup_user">Sign Up</button>
        <button class="button profile" action="/profile">Profile</button>
      </div>
    </header>
    <main>
      <div class="container">
        <div class="hero-section">
          <div class="hero-content">
            <h1>
              WELCOME TO<br>SHRAMSETU
            </h1>
            
            <div class="hero-buttons">
              <button class="button button-outline hireworker">Hire Worker</button>
              <button class="button button-outline  findjob">Find Job</button>
            </div>
          </div>
          <div class="card-container">
              <div class="image-slider">
                <div class="image-wrapper"><img src="/images/carpenter.jpg" alt="Carpenter"></div>
                <div class="image-wrapper"><img src="/images/plumber.jpg" alt="Plumber"></div>
                <div class="image-wrapper"><img src="/images/painter.jpg" alt="Painter"></div>
              </div>
            <button class="arrow right" onclick="scrollRight()">></button>
          </div>
        </div>
      </div>
    </main>
  </div>
  <div class="container1">
    <div class="image-sections">
      <!-- Top section - gray background with coral area on right -->
      <div class="section top">
        <div class="gray-box">
          worker
        </div>
        <div class="red-box img">
          <img src="/images/worker.webp">
        </div>
      </div>
      
      <!-- Bottom section - coral area on left with gray on right -->
      <div class="section bottom">
        <div class="red-box">
          <img src="/images/customer-interaction.jpg">
        </div>
        <div class="gray-box">
          customer
        </div>
      </div>
    </div>
  </div>

  <div class="ending">hello</div>
  <script src="script/landingpage.js"></script>
</body>
</html>    