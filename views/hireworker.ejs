<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SHRAMSETU SERVICES</title>
  <style>
    /* Reset and base styles */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: ui-sans-serif, system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
}

body {
  font-size: 16px;
  line-height: 24px;
  font-weight: 400;
  color: white;
  background-color: #1E2D3D;
  min-height: 100vh;
}

/* Main container */
.container {
  display: flex;
  min-height: 100vh;
}

/* Form section (left side) */
.form-section {
  flex: 1;
  display: flex;
  flex-direction: column;
  padding: 40px;
}

.title-container {
  margin-top: 24px;
  margin-bottom: 64px;
}

.main-title {
  font-size: 36px;
  font-weight: 700;
  line-height: 40px;
  letter-spacing: -0.9px;
}

.subtitle {
  font-size: 20px;
  font-weight: 500;
  line-height: 36px;
  margin-bottom: 32px;
}

form {
  display: flex;
  flex-direction: column;
  flex-grow: 1;
}

.textarea-container {
  flex-grow: 1;
  margin-bottom: 40px;
}

textarea {
  width: 100%;
  height: 160px;
  min-height: 80px;
  padding: 24px;
  background-color: rgb(226, 232, 240);
  border: none;
  border-radius: 16px;
  font-size: 20px;
  line-height: 28px;
  resize: none;
}

textarea:focus {
  outline: none;
}

.file-label {
  display: inline-block;
  padding: 10px 20px;
  background-color: #ffce9a;
  color: black;
  font-size: 14px;
  font-weight:500;
  cursor: pointer;
  border: none;
  border-radius: 9999px;
  transition: background-color 0.3s ease;
  white-space: nowrap;
}

.Upload {
  display: inline-block;
  padding: 10px 20px;
  background-color: #ffce9a;
  color: black;
  font-size: 14px;
  font-weight:500;
  cursor: pointer;
  border: none;
  border-radius: 9999px;
  transition: background-color 0.3s ease;
  white-space: nowrap;
}

.file-input{
  display: none;
}
.buttons-container {
  display: flex;
  gap: 16px;
  margin-bottom: 40px;
}

.btn {
  display: flex;
  align-items: center;
  justify-content: center;
  background-color:#ffce9a;
  color: rgb(0, 0, 0);
  font-size: 14px;
  font-weight: 500;
  border: none;
  border-radius: 9999px;
  cursor: pointer;
  transition: background-color 0.15s ease;
  height: 44px;
  white-space: nowrap;
  padding: 24px;
  text-align: center;
}

.btn:hover {
  background-color: rgb(203, 213, 225);
}

.next-btn {
  padding-left: 40px;
  padding-right: 40px;
}

.attach-btn {
  text-align: center;
  line-height: 20px;
}

.service-info {
  font-size: 20px;
  line-height: 28px;
  margin-bottom: 80px;
}

/* Image section (right side) */
.image-section {
  flex: 1;
  background-image: url("./images/pexels-gowtham-agm-609630353-18947381.jpg");
  background-size: cover;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24px;
  line-height: 32px;
  color: rgb(51, 65, 85);
}
.submit{
  display: none;
}

  .worker-type-display {
    margin-bottom: 15px; 
  }

/* Responsive styles */
@media (max-width: 768px) {
  .container {
    flex-direction: column;
  }
  
  .image-section {
    min-height: 300px;
  }
}

  </style>
</head>
<body>
  <div class="container">
    <!-- Left side - Form content -->
    <div class="form-section">
      <div class="title-container">
        <h1 class="main-title">SHRAMSETU<br>SERVICES</h1>
      </div>

      <% if (workerType) { %>
            <div class="worker-type-display">
                You are posting a job for a: <strong><%= workerType %></strong>
            </div>
        <% } %>
      <h2 class="subtitle">Tell us what you want us to do</h2>

      <form id="serviceForm" method="post" action="/uploadproblem" enctype="multipart/form-data">
         <input type="hidden" name="worker" value="<%= workerType %>">
        <div class="textarea-container">
          <textarea placeholder="Description of problem" id="problemDescription" name="content"></textarea>
        </div>

        
        <div class="buttons-container">
          <label for="fileUpload" class="file-label">Choose File</label>
          <input type="file" id="fileUpload" class="file-input" name="image">
          <button type="submit" class="btn next-btn">Submit</button>
        </div>
      </form>

        <div class="service-info">
          Will write about company's services
        </div>
    </div>

    <!-- Right side - Image placeholder -->
    <div class="image-section">
    </div>
  </div>
</body>
</html>
